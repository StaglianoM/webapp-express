{
  "name": "webapp-express",
  "version": "1.0.0",
  "description": "SELECT AVG(`vote`) as `avg_vote`, `movies`.`id`\r FROM `movies`\r JOIN `reviews`\r ON `movies`.`id` = `reviews`.`movie_id`\r GROUP BY `movies`.`id`;",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --env-file=.env.local --watch app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mysql2": "^3.12.0"
  }
}
